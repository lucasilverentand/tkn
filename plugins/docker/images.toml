match = "docker images"

[optimize]
optimize_stderr = true
truncate = "top"
max_lines = 50
strip = [
    "^\\s*$",
    "<none>\\s+<none>",      # untagged intermediate images
]

# Collapse multi-space column padding to single tab
[[optimize.replace]]
pattern = "  +"
replacement = "\t"
