match = "docker build"

[transform]
add = ["--progress=plain|--progress"]
savings_factor = 5.0

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 200
keep = [
    "(?i)error",
    "(?i)warning",
    "^#\\d+ ERROR",
    "exporting to image",
    "naming to ",
]
