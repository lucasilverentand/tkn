match = "grep"

[transform]
add = ["--color=never|--color|--color=auto|--color=always"]
savings_factor = 1.2

[optimize]
optimize_stderr = true
truncate = "top"
strip = [
    "^Binary file .* matches$",
    "Permission denied",
    "^grep: ",
    "^\\s*$",
]
max_lines = 50
