match = "grep"

[optimize]
truncate = "top"
strip = [
    "^Binary file .* matches$",
    "Permission denied",
]
max_lines = 50

# Strip absolute home directory prefix from file paths
[[optimize.replace]]
pattern = "^/(Users|home)/[^/]+/"
replacement = ""
