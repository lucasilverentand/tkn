match = "git status"

[transform]
add = ["--short|-s"]
savings_factor = 2.5

[optimize]
strip = [
    "^\\s*$",
    "(?i)\\.DS_Store$",
    "(?i)Thumbs\\.db$",
    "^\\s+\\(use \"git ",
    "^\\s+\\(all conflicts fixed",
]
max_lines = 100
