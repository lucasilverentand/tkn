match = "git diff"

[optimize]
strip = [
    "^diff --git ",
    "^index [a-f0-9]",
    "^(old|new) mode ",
    "^similarity index ",
    "^rename (from|to) ",
    "^copy (from|to) ",
]
max_bytes = 32768
