match = "git commit"

[optimize]
optimize_stderr = true
truncate = "top"
strip = [
    "^ (create|delete|copy) mode \\d+",
    "^ rename .+ => .+",
    "^ .+\\|\\s+\\d+",
    "^[→✓✗╭╰│├└─]",
    "^Warning: can't set",
    "^\\s*mode change \\d+",
]
max_lines = 50
