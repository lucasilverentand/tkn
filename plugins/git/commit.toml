match = "git commit"

[optimize]
truncate = "top"
strip = [
    "^ (create|delete|copy) mode \\d+",
    "^ rename .+ => .+",
    "^ .+\\|\\s+\\d+",
    "^[→✓✗╭╰│├└─]",
    "^Warning: can't set",
]
max_lines = 50
