match = "git log"

[transform]
add = [
    "--oneline|--pretty|--format",
    "-20|-n|--max-count",
]
savings_factor = 10.0

[optimize]
truncate = "top"
strip = [
    "^commit [a-f0-9]",
    "^Author:",
    "^Date:",
    "^Merge:",
]
max_lines = 200
