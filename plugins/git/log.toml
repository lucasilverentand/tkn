match = "git log"

[transform]
add = [
    "--oneline|--pretty|--format",
    "-20|-n|--max-count",
]

[optimize]
truncate = "top"
strip = [
    "^commit [a-f0-9]",
    "^Author:",
    "^Date:",
    "^Merge:",
]
max_lines = 200
