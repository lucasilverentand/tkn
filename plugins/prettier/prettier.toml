match = "prettier"

[transform]
add = ["--no-color|--color"]

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 150
strip = [
    "^Checking formatting\\.\\.\\.",
    "^All matched files use Prettier code style",
    "^\\s*$",
]
