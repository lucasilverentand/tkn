match = "bun install"

[optimize]
truncate = "bottom"
max_lines = 150
strip = [
    # Package resolution/download progress
    "^\\$ bun install",
    "^Resolving\\.\\.\\.",
    "^Resolved,? ",
    "^\\+ .+@\\d",           # individual package added lines
    "^Saved lockfile",
]
