match = "bun build"

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 200
keep = [
    "(?i)error",
    "(?i)warn",
    "^\\s*\\d+(\\.\\d+)?\\s*(kb|mb|b)\\b",  # size summary lines
]
