match = "bun test"

[optimize]
truncate = "bottom"
max_lines = 200
strip = [
    # Debug/trace log lines from test output
    "^\\[\\S+\\] conversation=\\S+ .+ (New message|Response compl|Error:)",
    # Passing test details (keep summary)
    "^\\(pass\\) ",
]
