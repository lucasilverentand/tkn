match = "terraform plan"

[transform]
add = ["-no-color"]
savings_factor = 1.5

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 200
strip = [
    "^\\s*$",
    "^Refreshing state\\.\\.\\.",
    "^\\s*# .+ will be read",
    "^\\s*# .+ has been ",
    "^\\s*<= data\\.",
]
