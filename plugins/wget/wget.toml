match = "wget"

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 100
strip = [
    "^\\d+K [\\. ]+\\d+%",         # progress bar lines
    "^Resolving ",
    "^Connecting to ",
    "^HTTP request sent",
    "^Length:",
    "^Saving to:",
    "^\\s*$",
]
