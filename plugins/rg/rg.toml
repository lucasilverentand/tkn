match = "rg"

[transform]
add = [
    "--no-heading",
    "--color=never",
]

[optimize]
truncate = "top"
strip = [
    "^Binary file .* matches$",
    "^--$",
    "^\\s*$",
]
max_lines = 75

# Shorten home directory paths
[[optimize.replace]]
pattern = "/(Users|home)/[^/]+/"
replacement = "~/"
