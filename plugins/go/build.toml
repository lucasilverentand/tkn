match = "go build"

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 200
keep = [
    "\\.go:\\d+:",           # file:line:col diagnostics
    "(?i)error",
    "(?i)undefined",
    "(?i)cannot ",
]
