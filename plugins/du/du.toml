match = "du"

[transform]
add = ["-h|-k|-m|-g|--human-readable|--si|--block-size"]

[optimize]
optimize_stderr = true
truncate = "top"
max_lines = 100
strip = [
    "Permission denied",
    "^\\s*$",
]
