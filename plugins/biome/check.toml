match = "biome check"

[transform]
add = ["--colors=off|--colors"]

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 150
strip = [
    "^\\s*$",
    "^Checked \\d+ files?",
    "^Found \\d+ errors?\\.$",
    "^\\[\\*\\] \\d+ fixable with",
]
