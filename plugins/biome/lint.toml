match = "biome lint"

[transform]
add = ["--colors=off|--colors"]

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 150
strip = [
    "^\\s*$",
    "^Found \\d+ errors?\\.$",
    "^\\[\\*\\] \\d+ fixable with",
    "^Checked \\d+ files?",
]
