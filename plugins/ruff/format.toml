match = "ruff format"

[transform]
add = ["--no-color|--color"]

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 150
strip = [
    "\\d+ files? left unchanged",
    "^All checks passed",
    "^\\s*$",
]
