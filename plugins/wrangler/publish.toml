match = "wrangler publish"

[optimize]
truncate = "bottom"
max_lines = 100
strip = [
    "^\\s*$",
    "^Uploading\\.\\.\\.",
    "^Total Upload:",
    "^Worker Startup Time:",
    "^Bundle Size:",
]
