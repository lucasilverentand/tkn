match = "lsof"

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 100
strip = [
    "^\\s*$",
]

# Collapse multi-space column padding to single tab
[[optimize.replace]]
pattern = "  +"
replacement = "\t"
