match = "pnpm run"

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 200
strip = [
    "^> .+@.+ ",            # lifecycle script header
    "^\\$ ",                 # echoed command
    "^ERR_PNPM",
]
