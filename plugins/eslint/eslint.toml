match = "eslint"

[transform]
add = ["--no-color|--color"]

[optimize]
optimize_stderr = true
truncate = "bottom"
max_lines = 150
strip = [
    "^\\s*$",
    "^✖ \\d+ problems?",           # summary line (redundant with individual errors)
    "^\\s+\\d+ errors? and \\d+ warnings?",
]
